import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MEU-INGRESSO',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
